# E2E Validation Report — Parallel Swarm + Cost-Aware Scheduling

**Date**: 2026-05-29
**Task**: `05-29-e2e-swarm-validation`
**Scope**: validate the parallel-swarm-scheduler + cost-aware-conductor-scheduling work (commits `ab70812`..`592e187`) — built, unit-tested (495 passed), but never exercised end-to-end against real git/runtime.

---

## Tier 1 — Deterministic real-git correctness (DONE)

### Method
- Confirmed the app boots healthy in mock mode: `REAL_CLI=false ./dev-local.sh` → backend `/api/codex/ready` = 200, frontend (4000) = 200.
- Built `backend/tests/test_swarm_integration.py` (6 scenarios, `@pytest.mark.slow`): drives the **real** `dispatch_batch` conductor tool against a **real temp git repo + real sqlite**, mocking only the subagent execution boundary (`dispatch_role` + `wait_for_active`) — and that mock **writes real files into each agent's real worktree**, so merge-back is genuine git.
- Independently audited by a second agent ("validate the validator"): confirmed no accidental stubbing of the code under test, real assertions, full tmp isolation (real `console.db`/repo untouched), no worktree leaks.

### Why this was the right Tier-1 vehicle
- Existing unit tests stubbed the WorktreeManager entirely; the full `dispatch_batch → per-agent worktree → sequential squash-merge-back` chain had **never run against real git**.
- The only project registered points at the live repo, so a full live conductor run risked the real working tree; a tmp-repo integration test removes that risk and is deterministic + free.

### Results — all green (`pytest tests/test_swarm_integration.py --runslow` → 6 passed)
| Scenario | Verified |
|---|---|
| Parallel fan-out + merge-back | both agents' files land on the issue branch via real squash-merge |
| **main not polluted** (PR3 regression) | `main` ref **and** tree byte-for-byte unchanged; agent files absent from main — under real git |
| Conflict reconcile | real conflict → `merge_status=conflict`, `conflicted_files` enumerated, non-empty diff, stop-on-first (earlier merge not rolled back), conflicting worktree kept |
| Upstream visibility | uncommitted issue-worktree artifact flushed before fork → visible in forked agent worktrees |
| Budget concurrency downscale | real peak-concurrency counter ≤ budget-allowed cap, off real persisted spend rows |
| Unlimited budget | keeps full configured cap |
| Over-budget soft semantics | batch runs to completion + merges, **no hard kill** |

### Verdict
**No production bug surfaced.** The swarm-safe merge primitive (PR3 main-pollution fix) and the soft-budget contracts hold end-to-end against real git + real sqlite. Tier 1 correctness is established.

Commit: `cef3034`.

---

## Tier 2 — Live UI walkthrough + real executor (NOT YET DONE)

Deferred — requires the user present:
- **Real-executor run** burns the user's API budget and needs the CLIs authenticated; the user authorized "one real run" but it should be watched.
- **Inducing `dispatch_batch`** depends on the conductor LLM brain (real API, MiniMax) deciding to fan out — non-deterministic; may need a crafted parallelizable issue or an injected steering message.
- Live UI evidence (WorkflowGraph parallel swimlane, `## COST / BUDGET` block, budget_warning/exceeded events) should be captured against a **throwaway project** (not the live repo) and screenshotted into `.walkthrough/`.

### Remaining Tier-2 checklist
- [ ] Throwaway repo + project + workspace + issue created via API
- [ ] Conductor induced to `dispatch_batch` (≥2 agents) in a live run
- [ ] Screenshots: parallel swimlane, budget block, steering events
- [ ] One authorized real-executor run: real agent artifacts merge correctly, cost accounting non-zero

---

## Bottom line
The **correctness** of the new orchestration is validated rigorously and safely (Tier 1). The **live visual/real-executor** confidence pass (Tier 2) is set up and scoped but intentionally left for a session where the user can authorize token spend and watch the run.
